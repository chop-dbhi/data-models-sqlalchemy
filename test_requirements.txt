# The below are copied from `requirements.txt`
docopt==0.6.2
Flask==0.11.1
future==0.15.2
requests==2.10.0
SQLAlchemy==1.0.11

# The below are required for ER diagram generation but can only be installed
# after the graphviz system library and not from a requirements file due to
# the `--install-option`s, which may have to be customized for your OS's
# graphviz installation location.
# pygraphviz==1.3.1 --install-option="--include-path=/usr/include/graphviz" --install-option="--library-path=/usr/lib/graphviz/"
ERAlchemy==1.0.13

# The below are only needed for tests as run in CircleCI. See `circle.yml`.
docker-py==1.8.1
python-coveralls==2.7.0
nose==1.3.7
